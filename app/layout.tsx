import type { Metadata } from "next";
import { AntdRegistry } from '@ant-design/nextjs-registry';

// Stores
import { ProviderRedux } from "@/tsipframework/tsip-base/reducers/providers";
import { saira } from '@/tsipframework/tsip-global/assets/fonts/index';

// Styles
import "./globals.scss";

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={saira.className}>
        <AntdRegistry>
          <ProviderRedux>
            {children}
          </ProviderRedux>
        </AntdRegistry>
      </body>
    </html>
  );
}
